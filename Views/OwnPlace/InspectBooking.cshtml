@using Microsoft.EntityFrameworkCore.Metadata.Internal
@{
    ViewData["Title"] = "Home Page";
}

@model HoldEvent.Models.Entities.Booking

<div class="text-center">
    <h1 class="display-4" style="font-weight:700">Inspect Booking</h1>
</div>

<form method="post" class="card p-4 mx-auto" style="max-width: 480px; box-shadow: 0 0 12px rgba(0,0,0,0.1); border-radius: 8px; background:#fff;">
    <input type="hidden" asp-for="BookingId" />

    <div class="mb-3">
        <label class="form-label">Day - Time Start</label>
        <input type="text" class="form-control" value="@Model.DateStart?.ToString("dd/MM/yyyy")" readonly />
    </div>

    <div class="mb-3">
        <label class="form-label">Day - Time End</label>
        <input type="text" class="form-control" value="@Model.DateEnd?.ToString("dd/MM/yyyy")" readonly />
    </div>

    <div class="mb-3">
        <label class="form-label">Day - Time Booking</label>
        <input type="text" class="form-control" value="@Model.BookingDate?.ToString("dd/MM/yyyy")" readonly />
    </div>

    <div class="mb-4">
        <label asp-for="Deposit" class="form-label">Total Price $</label>
        <input type="number" class="form-control" asp-for="Deposit" readonly />
        <span asp-validation-for="Deposit" class="text-danger"></span>
    </div>

    <div class="d-flex justify-content-between gap-2">
        <button type="submit" name="BookingStatus" value="Approved" class="btn btn-success flex-fill">Approve</button>
        <button type="submit" name="BookingStatus" value="Rejected" class="btn btn-warning flex-fill">Reject</button>
        <button type="submit" formmethod="get" asp-area="" asp-controller="OwnPlace" asp-action="ListBooking" class="btn btn-danger flex-grow-1">Cancel</button>
    </div>
</form>
